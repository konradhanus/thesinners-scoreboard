// import React, { useState } from "react";
// import { connect } from 'react-redux';

const Language = (props: any) => {
    const pl = () =>{
        props.setNumber('10');
    }

    const eng = () =>{
        props.setNumber('100');
    }
    return <>aaa</>

}

//  export default connect(null, actionCreators)(Language);
