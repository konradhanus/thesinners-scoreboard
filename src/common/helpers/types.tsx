export interface Winners {
  icon: string;
  place: string;
  avatar: string;
  name: string;
  level: string;
  points: number;
}

export interface Others {
  place: string;
  avatar: string;
  name: string;
  level: string;
  points: number;
}

export interface Without {
  place: string;
  avatar: string;
  name: string;
  points: number;
}
